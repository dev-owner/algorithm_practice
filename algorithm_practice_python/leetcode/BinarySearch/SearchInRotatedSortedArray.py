class SearchInRotatedSortedArray:
    def search(self, nums, target):
        """
        :type nums: List[int]
        :type target: int
        :rtype: int
        """
        # rotated array
        # loop - O(n)
        # sort + binary - O(nlogn)
        # binary - O(logn) - how ?  (how can i achieve sort process in O(logn))


        return -1
